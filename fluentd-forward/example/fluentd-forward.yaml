kind: user
metadata:
  name: fluentd-forward
spec:
  roles: ['fluentd-forward']
version: v2
---
kind: role
metadata:
  name: fluentd-forward
spec:
  allow:
    rules:
      - resources: ['events']
        verbs: ['list','read']
version: v3
