
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: identities.auth.teleport.dev
spec:
  group: auth.teleport.dev
  names:
    kind: Identity
    listKind: IdentityList
    plural: identities
    singular: identity
  scope: Namespaced
  versions:
  - name: v8
    schema:
      openAPIV3Schema:
        description: Identity is the Schema for the identities API Identity resource
          describes a request for Teleport identity file to be generated.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: IdentitySpec defines the desired state of Identity object.
            properties:
              secretName:
                description: SecretName is a name of the secret resource to store
                  the Teleport identity contents. Secret resource is located in the
                  same namespace as Identity resource.
                type: string
              ttl:
                description: TTL is a duration of TLS/SSH certificates lifetime.
                type: string
              username:
                description: Username is a name of the Teleport user for whom we generate
                  an identity.
                type: string
            required:
            - secretName
            - username
            type: object
          status:
            description: IdentityStatus defines the observed state of Identity object.
            properties:
              needRenewal:
                description: NeedRenewal indicates that identity secret must be re-generated.
                type: boolean
            required:
            - needRenewal
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
